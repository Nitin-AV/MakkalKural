name: smart_civic_connect
description: "A new Flutter project."
version: 1.0.0+1

environment:
  sdk: ^3.6.1
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^4.4.0
  firebase_auth: ^6.1.4
  cloud_firestore:
  firebase_storage:
  supabase_flutter: ^2.5.6
  shared_preferences: ^2.2.2
  image_picker: ^1.0.7
  geolocator: ^10.1.0
  http: ^1.2.0
  url_launcher: ^6.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true
  assets:
    - images/logo.png
    - images/logo1.png
    - images/icon.png